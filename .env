# Copy this file to ".env" and fill in your own values.
# app.py loads this automatically via python-dotenv.

# Your LLM provider's API key (OpenAI, or any OpenAI-compatible endpoint).
OPENAI_API_KEY=a0Bfv000002VDSsEAO

# Base URL for the API. Leave as-is for OpenAI, or point it at a
# compatible provider / self-hosted gateway (e.g. Azure OpenAI, vLLM, etc).
OPENAI_BASE_URL=https://47v4us7kyypinfb5lcligtc3x40ygqbs.lambda-url.us-east-1.on.aws/v1/

# Chat model to use for classification, extraction, and response generation.
OPENAI_MODEL=gpt-4o-mini